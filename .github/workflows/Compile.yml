name: Compile

on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      ssh:
        description: 'SSH connection to Actions'
        required: false
        default: 'false'

env:
  UPLOAD_COWTRANSFER: false
  RELEASE: true
  TZ: Canada/Atlantic
  FILE_NAME: OpenWrt-S905X3

jobs:
  build:
    runs-on: ubuntu-20.04
    name: Build for ${{ matrix.flippy_name }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - flippy_name: First
            release_name: OpenWrt_S905X3_5.15.15_69+.img
          - flippy_name: Second
            release_name: OpenWrt_S905X3_5.10.92_69+.img
          - flippy_name: Third
            release_name: OpenWrt_S905X3_5.4.172_69+o.img

    env:
      FLIPPY_NAME: ${{ matrix.flippy_name }}   
      RELEASE_NAME: ${{ matrix.release_name }}
    steps:
    - name: Checkout
      uses: actions/checkout@main
      
    - name: Initialization environment
      run: |
        version=$(curl -s "https://api.github.com/repos/Lvellios/Compile-Kernel/releases/latest" | awk -F '"' '/tag_name/{print $4}')

        echo "version=$version"  >> $GITHUB_ENV
        echo "DATE=$(date "+%Y-%m-%d %H:%M:%S")"  >> $GITHUB_ENV
 
        sudo chmod  -R 777 /opt
        cp -r ${FLIPPY_NAME}/opt/* /opt

    - name: Download
      run: |
        cd /opt/packit
        
        wget  https://github.com/Lvellios/Compile-Kernel/releases/download/$version/openwrt-armvirt-64-default-rootfs.tar.gz
                
    - name: Check The Files
      run: |
        cd /opt/packit
        ls
                
    - name: Install The Packages
      run: |
        sudo rm -rf /etc/apt/sources.list.d/* /usr/share/dotnet /usr/local/lib/android /opt/ghc
        sudo -E apt-get -qq update
        sudo -E apt-get -qq install xz-utils btrfs-progs gawk zip unzip curl dosfstools  uuid-runtime
        sudo -E apt-get -qq install git  git-core
        sudo -E apt-get -qq install pigz
        sudo -E apt-get -qq autoremove --purge
        sudo -E apt-get -qq clean
    
    - name: Build The Firmware
      run: |
        sudo chmod  -R 777 /opt
        cd /opt/packit
        sudo chmod +x mk_s905x3_multi.sh
        sudo ./mk_s905x3_multi.sh
        
    - name: Copy The Update Files
      run: |
        cd /opt/packit
        sudo cp files/openwrt-update-amlogic output/openwrt-update-amlogic

    - name: Pigz -9
      id: pigz
      run: |
        sudo chmod -R 777 /opt/packit/output
        cd /opt/packit/output
        sudo pigz -9 $RELEASE_NAME

        echo "::set-output name=status::success"

    - name: Check The Files
      run: |
        cd /opt/packit/output
        ls

    - name: Check The Space Usage
      if: (!cancelled())
      run: df -hT

    - name: Push The Tag
      id: tag
      run: |
        echo "::set-output name=release_tag::$(date +"%m%d%Y%H")"

    - name: Create The Release
      id: release
      uses: ncipollo/release-action@v1
      with:
        name: ${{ env.FILE_NAME }}
        allowUpdates: true
        tag: ${{ steps.tag.outputs.release_tag }}
        commit: main
        token: ${{ secrets.RELEASES_TOKEN }}
        body: |
          The Firmware of OpenWrt For Amlogic S905X3
        artifacts: "/opt/packit/output/*"